{
  "compilerOptions": {
    "target": "es6", // TODO: I don't know what this is
    "module": "commonjs", // TODO: I don't know what this is
    "outDir": "./dist",
    "strict": true,
    "esModuleInterop": true, // TODO: I don't know what this is
    "baseUrl": "./",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*.ts"], // TODO: I don't know what this is
  "exclude": ["node_modules"],
  "ts-node": {
    "require": ["tsconfig-paths/register"] // tsconfig-paths is a package that allows us to use the paths we defined in tsconfig.json
  }
}
